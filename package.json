{
  "name": "compiler-phases-demo-root",
  "version": "1.0.0",
  "description": "Root package for Compiler Phases Demonstration Tool",
  "scripts": {
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "build-client": "npm run build --prefix client",
    "build": "npm run install-client && npm run install-server && npm run build-client",
    "vercel-build": "npm run install-client && npm run build-client",
    "start": "node server/app.js"
  }
}
